function question5() {
  return `O texto informa sobre a sintaxe de uma função, que é definida por uma Keyword seguida do nome e parenteses ().
   Nomes de funções podem conter letras, dígitos, underscore e cifrão.
   Dentro dos parenteses são declarados parâmetros, que são separados por ','.
   O código a ser executado pela função é colocado dentro de '{}'.
   Parâmetros da função são listados dentro de parenteses () na definição da função.
   Argumentos da função são os valores recebidos pela função quando ela é chamada.
   Dentro da função os argumentos (parâmentros) comportam-se como variáveis locais.
   Uma funcão é parecida com Procedures ou Subrotinas em outras linguagems de progração.`;
}

export default question5;
